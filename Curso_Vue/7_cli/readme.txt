# tener instalado node
node -v

# instalar el cli (no importa el directorio, es global)
npm install -g @vue/cli

# verificar la version
vue --version

# crear proyecto nuevo
vue create 7_cli

# ir al directorio del proyecto creado
cd 7_cli

# correr
npm run serve